# NodeJS Interview Questions

## 1. What is it?
Common junior interview questions focusing on event loops, modules, file systems, and packaging.

## 2. Why do we use it?
To confirm a developer's understanding of single-threaded asynchronous runtime server concepts, modules formats, and filesystem operations.

## 3. How does it work?
- **Analogy**: Flashcards for Node.js runtimes, Event Emitter classes, and non-blocking APIs.
- **Mechanism**: The feature parses inputs or operations synchronously or asynchronously, executing compiled instructions and returning output responses or state modifications.

## 4. Where is it used?
Backend developer and node server technical interviews.

## 5. How do we build with it?
```json
Why is Node.js single-threaded? -> It handles concurrency using async event loops rather than multiple threads.
```

- **Expected Output**: Prepares you for server runtime questions.
- **Best Practice / Rule**: Always use path.join instead of string concatenation to join folder paths.
