# MongoDB Interview Questions

## 1. What is it?
Common junior interview questions focusing on SQL vs NoSQL, collections, documents, and mongoose schemas.

## 2. Why do we use it?
To test document-based database querying strategies, indexing performance operations, and schema integration via Mongoose ODM.

## 3. How does it work?
- **Analogy**: Flashcards for NoSQL document models, indexes speed, and aggregations pipelines.
- **Mechanism**: The feature parses inputs or operations synchronously or asynchronously, executing compiled instructions and returning output responses or state modifications.

## 4. Where is it used?
Full-stack and database design technical screens.

## 5. How do we build with it?
```json
SQL vs NoSQL? -> SQL uses fixed tables/rows; NoSQL stores flexible JSON-like documents.
```

- **Expected Output**: Prepares you for database technical interview loops.
- **Best Practice / Rule**: Indexes speed up database read operations but slow down write operations.
