# CSS Interview Questions

## 1. What is it?
Common junior interview questions focusing on Box Model, Flexbox, Grid, and specificity.

## 2. Why do we use it?
To test a candidate's practical command of layout engines (Flexbox, Grid), box-model boundaries, responsive design rules, and specificity resolution.

## 3. How does it work?
- **Analogy**: Flashcards for CSS layouts, specificity values, and positioning rules.
- **Mechanism**: The feature parses inputs or operations synchronously or asynchronously, executing compiled instructions and returning output responses or state modifications.

## 4. Where is it used?
Layout and UI engineering technical screens.

## 5. How do we build with it?
```json
Explain the box model -> Margin, Border, Padding, Content.
```

- **Expected Output**: Prepares you for technical layout and styling questions.
- **Best Practice / Rule**: ID selectors have higher specificity than class selectors.
