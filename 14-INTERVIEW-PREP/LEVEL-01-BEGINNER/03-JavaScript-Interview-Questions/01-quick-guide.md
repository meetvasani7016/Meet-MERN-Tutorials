# JavaScript Interview Questions

## 1. What is it?
Common junior developer interview questions focusing on closures, event loops, promises, and scopes.

## 2. Why do we use it?
To evaluate core scripting expertise including scopes, closures, asynchronous event loops, and Promises, which form the logic core of modern web apps.

## 3. How does it work?
- **Analogy**: Flashcards for core JavaScript behaviors, closures, scopes, and async flows.
- **Mechanism**: The feature parses inputs or operations synchronously or asynchronously, executing compiled instructions and returning output responses or state modifications.

## 4. Where is it used?
Core JavaScript and frontend logic interviews.

## 5. How do we build with it?
```json
What is a closure? -> A function that remembers its outer variables even after the outer function has executed.
```

- **Expected Output**: Prepares you for core JavaScript scripting questions.
- **Best Practice / Rule**: var is function-scoped; let and const are block-scoped.
