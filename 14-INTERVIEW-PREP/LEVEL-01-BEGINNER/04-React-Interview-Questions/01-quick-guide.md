# React Interview Questions

## 1. What is it?
Common junior interview questions focusing on Virtual DOM, Hooks, state vs props, and performance.

## 2. Why do we use it?
To assess familiarity with modern single-page applications structures, Virtual DOM diffing, component state lifecycles, and Hooks rules.

## 3. How does it work?
- **Analogy**: Flashcards for React rendering rules, state variables, props, and hooks.
- **Mechanism**: The feature parses inputs or operations synchronously or asynchronously, executing compiled instructions and returning output responses or state modifications.

## 4. Where is it used?
React and frontend framework interview loops.

## 5. How do we build with it?
```json
What is Virtual DOM? -> A lightweight copy of the real DOM kept in memory, synced via diffing.
```

- **Expected Output**: Prepares you for React framework interview checks.
- **Best Practice / Rule**: Props are read-only; State is managed within the component and can change.
