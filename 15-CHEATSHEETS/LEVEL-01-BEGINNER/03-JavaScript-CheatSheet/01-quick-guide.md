# JavaScript CheatSheet

## In One Line
Print-friendly, compact reference for variables, loop syntaxes, array methods, and async/await.

## Think Like This
Refinement 1 compliant: Variable keywords, loops, functional expressions, and DOM selectors. Look once, remember forever.

## Example
```json
const double = x => x * 2; // Arrow Function
```

## Result
Instant script syntax memory booster.

## Remember
Variables defined with const cannot be reassigned (but their object values can be modified).
