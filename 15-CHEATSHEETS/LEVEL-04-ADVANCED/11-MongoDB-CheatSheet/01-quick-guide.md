# MongoDB CheatSheet

## In One Line
Print-friendly, compact reference for mongosh query operators, CRUD operations, index calls, and aggregation filters.

## Think Like This
Look once, remember forever: mongosh query commands and mongoose schema models.

## Example
```js
db.users.find({ age: { $gte: 21 } })
```

## Result
Enables fast NoSQL database query lookup checks.

## Remember
Always write updates wrapping $set operators to avoid replacing entire document logs.
