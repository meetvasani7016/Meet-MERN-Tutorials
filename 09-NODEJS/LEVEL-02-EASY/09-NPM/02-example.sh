# Install a local dependency package
npm install lodash

# Install a package as devDependency (only for local development)
npm install -D nodemon

# Install a global package (CLI tool)
# npm install -g target-package