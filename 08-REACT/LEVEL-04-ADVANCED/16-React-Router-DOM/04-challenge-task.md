# Challenge Task: React Router DOM

## Objective
Push your understanding further by building a slightly more complex layout.

## Challenge Instructions
Implement dynamic routing parameters using `:id` values and read them inside target pages using the `useParams` hook.
