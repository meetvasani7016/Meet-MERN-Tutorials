# Summary: HTML Audio

Here is a quick cheat sheet of what we covered in this lesson:

* Embed sound using the `<audio>` tag.
* `controls` shows play/pause buttons.
* Use `<source>` tags to support MP3 and OGG fallback types.
