# Challenge Task: HTML Accessibility

## Objective
Push your understanding further by building a slightly more complex layout.

## Challenge Instructions
Add an icon-only button (like 'X' for close) and configure it with an `aria-label` attribute describing its function.
