# Challenge Task: DOM Selection

## Objective
Push your understanding further by building a slightly more complex layout.

## Challenge Instructions
Use `querySelectorAll()` to select all paragraph items. Loop through the returned collection using `forEach` and log the text content (`innerText`) of each paragraph to the console.
