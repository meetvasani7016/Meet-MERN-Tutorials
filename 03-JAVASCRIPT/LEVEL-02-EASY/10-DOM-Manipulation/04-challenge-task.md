# Challenge Task: DOM Manipulation

## Objective
Push your understanding further by building a slightly more complex layout.

## Challenge Instructions
Create a div container with `id="container"`. Write script codes to programmatically create a button element (`createElement`), set its text to 'Click Me', and append it into the container using `appendChild`.
