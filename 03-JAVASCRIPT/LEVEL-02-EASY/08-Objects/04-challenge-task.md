# Challenge Task: JavaScript Objects

## Objective
Push your understanding further by building a slightly more complex layout.

## Challenge Instructions
Write an object representing a circle. It should have a radius property and a method named `calculateArea`. Inside the method, use `this.radius` to calculate and return the area (Math.PI * radius * radius).
