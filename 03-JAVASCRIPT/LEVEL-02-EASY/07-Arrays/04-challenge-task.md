# Challenge Task: JavaScript Arrays

## Objective
Push your understanding further by building a slightly more complex layout.

## Challenge Instructions
Given an array of numbers `[10, 20, 30, 40, 50]`, use `filter()` to get numbers greater than 25, and then use `map()` to double those filtered values. Print the final resulting array.
