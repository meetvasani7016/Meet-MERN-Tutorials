# Challenge Task: JavaScript Promises

## Objective
Push your understanding further by building a slightly more complex layout.

## Challenge Instructions
Create a promise that checks a balance. If balance is greater than 50, resolve with 'Approved', otherwise reject with 'Insufficient funds'. Run it and handle both outcomes using `.then()` and `.catch()`.
