# Challenge Task: Web Storage

## Objective
Push your understanding further by building a slightly more complex layout.

## Challenge Instructions
Write script codes to store a user object `{ name: 'Alice', score: 100 }` in localStorage, and retrieve and parse it back into an active object without triggering syntax errors.
