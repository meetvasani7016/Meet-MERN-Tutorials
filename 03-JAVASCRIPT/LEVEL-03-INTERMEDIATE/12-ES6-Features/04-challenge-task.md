# Challenge Task: ES6 Features

## Objective
Push your understanding further by building a slightly more complex layout.

## Challenge Instructions
Demonstrate optional chaining: Create a nested object `user` that does not contain a `profile` property. Attempt to log `user.profile.bio` directly vs `user?.profile?.bio` and explain the difference.
