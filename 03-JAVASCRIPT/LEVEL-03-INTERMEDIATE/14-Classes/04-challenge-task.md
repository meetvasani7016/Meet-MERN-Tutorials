# Challenge Task: JavaScript Classes

## Objective
Push your understanding further by building a slightly more complex layout.

## Challenge Instructions
Implement inheritance: Create a subclass `ElectricCar` that extends `Car` and adds a constructor property for `batteryLife`. Call `super()` inside the constructor to pass brand and speed parameters.
