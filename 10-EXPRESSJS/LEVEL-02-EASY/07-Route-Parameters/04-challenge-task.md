# Challenge Task: Route Parameters

## Objective
Push your understanding further by building a slightly more complex layout.

## Challenge Instructions
Build a route containing multiple params (e.g. '/users/:userId/books/:bookId') and parse both.
