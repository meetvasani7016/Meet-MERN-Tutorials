# Challenge Task: Git Commit

## Objective
Push your understanding further by building a slightly more complex layout.

## Challenge Instructions
Make an edit inside `index.html` (e.g., change header text), stage the change, and commit it with a message: `git commit -am "Update heading text"`. Explain what the `-am` flag combination does.
