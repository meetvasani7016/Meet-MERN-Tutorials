# Practice Task: Git Introduction

## Objective
Apply what you've learned in this lesson by completing a basic exercise.

## Step-by-Step Instructions
Check if Git is installed on your computer. Open a terminal and run `git --version`. Set your developer identity with your name and email using `git config --global user.name` and `git config --global user.email`.
