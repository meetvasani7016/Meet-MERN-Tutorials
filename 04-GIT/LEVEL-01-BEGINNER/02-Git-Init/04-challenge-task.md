# Challenge Task: Git Init

## Objective
Push your understanding further by building a slightly more complex layout.

## Challenge Instructions
Use a command line flag to find the hidden `.git` folder (e.g., `ls -la` or `dir /a`) and check what folders are created inside it.
