# Challenge Task: Git Pull

## Objective
Push your understanding further by building a slightly more complex layout.

## Challenge Instructions
Explain the differences between running `git fetch` (downloads metadata only) vs `git pull` (downloads and merges code).
