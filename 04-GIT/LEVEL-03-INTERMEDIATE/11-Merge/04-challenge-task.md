# Challenge Task: Git Merge

## Objective
Push your understanding further by building a slightly more complex layout.

## Challenge Instructions
Merge a branch without fast-forwarding, creating a merge commit explicitly using `git merge --no-ff <branch-name>`.
